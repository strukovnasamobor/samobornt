<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <!-- Android 14 and older: the window is not edge-to-edge and the system
             paints the status bar itself, from this attribute. -->
        <item name="android:statusBarColor">@color/statusBar</item>
        <!-- Android 15+ (targetSdk 36) ignores the attribute above, so there
             MainActivity paints the status bar strip itself. windowBackground
             deliberately stays untouched: it also backs the navigation bar,
             which is meant to keep the system default. -->
        <!-- keep the clock and icons light against the dark red -->
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- same bar treatment during the splash, so it does not flash a
             different colour on the way to MainActivity -->
        <item name="android:statusBarColor">@color/statusBar</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>